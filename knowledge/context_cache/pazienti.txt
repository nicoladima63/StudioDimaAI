QUERY: pazienti

=== PRIORITY FILES ===
- client_v2/src/store/pazienti.store.tsx | role=state | score=42 | structural=36 | query=6 | matched=pazienti
- server_v2/api/v2_pazienti.py | role=api | score=29 | structural=19 | query=10 | matched=pazienti
- server_v2/services/pazienti_service.py | role=service | score=29 | structural=19 | query=10 | matched=pazienti
- client_v2/src/features/pazienti/pages/PazientiPage.tsx | role=page | score=23 | structural=17 | query=6 | matched=pazienti
- client_v2/src/features/pazienti/pages/RichiamiPage.tsx | role=page | score=21 | structural=15 | query=6 | matched=pazienti
- client_v2/src/features/pazienti/services/richiami.service.ts | role=service | score=20 | structural=14 | query=6 | matched=pazienti
- client_v2/src/features/pazienti/components/RichiamoStatus.tsx | role=component | score=20 | structural=14 | query=6 | matched=pazienti
- client_v2/src/features/pazienti/services/marketing.service.ts | role=service | score=20 | structural=14 | query=6 | matched=pazienti
- client_v2/src/features/pazienti/components/PazientiTable.tsx | role=component | score=18 | structural=12 | query=6 | matched=pazienti
- client_v2/src/features/pazienti/components/FiltriPazienti.tsx | role=component | score=17 | structural=11 | query=6 | matched=pazienti
- client_v2/src/features/pazienti/pages/SlotLiberiPage.tsx | role=page | score=17 | structural=11 | query=6 | matched=pazienti
- client_v2/src/features/pazienti/components/ModalRichiamoGestione.tsx | role=component | score=16 | structural=10 | query=6 | matched=pazienti
- client_v2/src/features/pazienti/components/RichiamoGestione.tsx | role=component | score=15 | structural=9 | query=6 | matched=pazienti
- client_v2/src/features/pazienti/components/BroadcastModal.tsx | role=component | score=15 | structural=9 | query=6 | matched=pazienti
- server_v2/scripts/seed_patient_education.py | role=script | score=12 | structural=2 | query=10 | matched=patient
- server_v2/scripts/verify_patient_plans.py | role=script | score=8 | structural=2 | query=6 | matched=patient

=== ARCHITECTURE ===

STATE
- client_v2/src/store/pazienti.store.tsx

API
- server_v2/api/v2_pazienti.py

SERVICE
- server_v2/services/pazienti_service.py
- client_v2/src/features/pazienti/services/richiami.service.ts
- client_v2/src/features/pazienti/services/marketing.service.ts

PAGE
- client_v2/src/features/pazienti/pages/PazientiPage.tsx
- client_v2/src/features/pazienti/pages/RichiamiPage.tsx
- client_v2/src/features/pazienti/pages/SlotLiberiPage.tsx

COMPONENT
- client_v2/src/features/pazienti/components/RichiamoStatus.tsx
- client_v2/src/features/pazienti/components/PazientiTable.tsx
- client_v2/src/features/pazienti/components/FiltriPazienti.tsx
- client_v2/src/features/pazienti/components/ModalRichiamoGestione.tsx
- client_v2/src/features/pazienti/components/RichiamoGestione.tsx
- client_v2/src/features/pazienti/components/BroadcastModal.tsx

SCRIPT
- server_v2/scripts/seed_patient_education.py
- server_v2/scripts/verify_patient_plans.py

=== SYMBOLS ===
- usePazienti | client_v2/src/store/pazienti.store.tsx | kind=hook | category=domain | score=63
- PazientiService | server_v2/services/pazienti_service.py | kind=class | category=core | score=46
- get_pazienti_paginated | server_v2/services/pazienti_service.py | kind=function | category=domain | score=39
- search_pazienti | server_v2/services/pazienti_service.py | kind=function | category=domain | score=39
- get_pazienti | server_v2/api/v2_pazienti.py | kind=function | category=domain | score=38
- search_pazienti | server_v2/api/v2_pazienti.py | kind=function | category=domain | score=38
- get_pazienti_stats | server_v2/api/v2_pazienti.py | kind=function | category=domain | score=38
- seed_patient_education | server_v2/scripts/seed_patient_education.py | kind=function | category=support | score=30
- getCampiAnagrafica | client_v2/src/features/pazienti/pages/PazientiPage.tsx | kind=component | category=support | score=26
- sortIcon | client_v2/src/features/pazienti/pages/RichiamiPage.tsx | kind=component | category=ui | score=24
- get_paziente_by_id | server_v2/services/pazienti_service.py | kind=function | category=domain | score=24
- _get_pazienti_dbf_path | server_v2/services/pazienti_service.py | kind=function | category=internal | score=24
- get_paziente | server_v2/api/v2_pazienti.py | kind=function | category=domain | score=23
- calcolaEta | client_v2/src/features/pazienti/components/FiltriPazienti.tsx | kind=function | category=support | score=22
- applicaFiltri | client_v2/src/features/pazienti/components/FiltriPazienti.tsx | kind=function | category=support | score=22
- hasGestionaleData | client_v2/src/features/pazienti/components/RichiamoStatus.tsx | kind=component | category=support | score=21
- getRichiamoType | client_v2/src/features/pazienti/components/RichiamoStatus.tsx | kind=component | category=domain | score=21
- calcolaProssimoRichiamo | client_v2/src/features/pazienti/components/RichiamoStatus.tsx | kind=component | category=domain | score=21
- decodificaTipoRichiamo | client_v2/src/features/pazienti/components/RichiamoStatus.tsx | kind=component | category=domain | score=21
- styled | client_v2/src/features/pazienti/components/RichiamoStatus.tsx | kind=component | category=ui | score=21
- carica | client_v2/src/features/pazienti/pages/SlotLiberiPage.tsx | kind=component | category=support | score=20
- calcolaProssimoRichiamo | client_v2/src/features/pazienti/components/ModalRichiamoGestione.tsx | kind=component | category=domain | score=19
- getStatusBadge | client_v2/src/features/pazienti/components/ModalRichiamoGestione.tsx | kind=component | category=ui | score=19
- handleForceRefresh | client_v2/src/features/pazienti/pages/PazientiPage.tsx | kind=component | category=support | score=16
- handleView | client_v2/src/features/pazienti/pages/PazientiPage.tsx | kind=component | category=support | score=16
- closeAnagraficaModal | client_v2/src/features/pazienti/pages/PazientiPage.tsx | kind=component | category=ui | score=16
- verify_plans | server_v2/scripts/verify_patient_plans.py | kind=function | category=support | score=15
- resetFiltri | client_v2/src/features/pazienti/pages/RichiamiPage.tsx | kind=component | category=support | score=14
- toggleSort | client_v2/src/features/pazienti/pages/RichiamiPage.tsx | kind=component | category=ui | score=14
- formatData | client_v2/src/features/pazienti/pages/RichiamiPage.tsx | kind=component | category=support | score=14
- handle_error | server_v2/api/v2_pazienti.py | kind=function | category=support | score=13
- validate_pagination_params | server_v2/api/v2_pazienti.py | kind=function | category=support | score=13
- handle_validation_error | server_v2/api/v2_pazienti.py | kind=function | category=support | score=13
- handle_database_error | server_v2/api/v2_pazienti.py | kind=function | category=support | score=13
- handle_validation_error | server_v2/api/v2_pazienti.py | kind=function | category=support | score=13
- handle_database_error | server_v2/api/v2_pazienti.py | kind=function | category=support | score=13
- set | client_v2/src/features/pazienti/components/FiltriPazienti.tsx | kind=component | category=support | score=11
- handleQuickSave | client_v2/src/features/pazienti/components/RichiamoStatus.tsx | kind=component | category=support | score=11
- formatData | client_v2/src/features/pazienti/pages/SlotLiberiPage.tsx | kind=function | category=support | score=11
- parseSlotDate | client_v2/src/features/pazienti/pages/SlotLiberiPage.tsx | kind=function | category=support | score=11
- togglePaziente | client_v2/src/features/pazienti/pages/SlotLiberiPage.tsx | kind=component | category=domain | score=10
- handleTemplateChange | client_v2/src/features/pazienti/components/BroadcastModal.tsx | kind=component | category=support | score=9
- handleSend | client_v2/src/features/pazienti/components/BroadcastModal.tsx | kind=component | category=support | score=9
- handleClose | client_v2/src/features/pazienti/components/BroadcastModal.tsx | kind=component | category=support | score=9
- handleTipoToggle | client_v2/src/features/pazienti/components/ModalRichiamoGestione.tsx | kind=component | category=ui | score=9
- handleSaveStatus | client_v2/src/features/pazienti/components/ModalRichiamoGestione.tsx | kind=component | category=support | score=9
- handleStatusChange | client_v2/src/features/pazienti/components/RichiamoGestione.tsx | kind=component | category=support | score=9
- handleSaveRichiamato | client_v2/src/features/pazienti/components/RichiamoGestione.tsx | kind=component | category=support | score=9
- handleSaveConfig | client_v2/src/features/pazienti/components/RichiamoGestione.tsx | kind=component | category=support | score=9
- _extract_paziente_data | server_v2/services/pazienti_service.py | kind=function | category=internal | score=9

=== RELEVANT CODE ===

--- C:\Users\NICOLA\Desktop\StudioDimaAI\client_v2\src\store\pazienti.store.tsx::usePazienti (lines 294-308) ---
export const usePazienti = () => {
  const store = usePazientiStore();
  
  return {
    pazienti: store.filteredPazienti,
    allPazienti: store.pazienti,
    isLoading: store.isLoading,
    error: store.error,
    searchTerm: store.searchTerm,
    search: store.searchPazienti,
    load: () => store.loadPazienti({ force: true }),
    loadAll: () => store.loadAllPazienti(),
    getById: store.getPazienteById
  };
};

--- C:\Users\NICOLA\Desktop\StudioDimaAI\server_v2\services\pazienti_service.py::PazientiService (lines 45-432) ---
class PazientiService(BaseService):
    
    def get_pazienti_paginated(self, page=1, per_page=50, filters=None) -> Dict[str, Any]:
        """Get paginated list of pazienti from DBF files."""
        try:
            # Get DBF file path using config manager
            pazienti_path = self._get_pazienti_dbf_path()
            
            if not os.path.exists(pazienti_path):
                self.logger.warning(f"Pazienti DBF file not found: {pazienti_path}")
                raise DatabaseError(f"Pazienti DBF file not found: {pazienti_path}")
            
            # Read DBF file directly using dbf library
            pazienti_raw = []
            with dbf.Table(pazienti_path, codepage='cp1252') as table:
                for record in table:
                    try:
                        # Extract fields using constants mapping - access directly by field name
                        paziente_id = clean_dbf_value(record[PAZIENTI_FIELDS['id']])
                        paziente_nome = clean_dbf_value(record[PAZIENTI_FIELDS['nome']])
                        
                        if paziente_id and paziente_nome:
                            # Extract all available fields
                            paziente_data = {
                                'id': str(paziente_id).strip(),
                                'nome': str(paziente_nome).strip()
                            }
                            
                            # Add optional fields if available
                            try:
                                if 'codice_fiscale' in PAZIENTI_FIELDS:
                                    cf = clean_dbf_value(record[PAZIENTI_FIELDS['codice_fiscale']])
                                    if cf:
                                        paziente_data['codice_fiscale'] = str(cf).strip()
                                        
                                if 'data_nascita' in PAZIENTI_FIELDS:
                                    data_nascita = clean_dbf_value(record[PAZIENTI_FIELDS['data_nascita']])
                                    if data_nascita:
                                        paziente_data['data_nascita'] = self._format_date_field(data_nascita)
                                        
                                if 'sesso' in PAZIENTI_FIELDS:
                                    sesso = clean_dbf_value(record[PAZIENTI_FIELDS['sesso']])
                                    if sesso:
                                        paziente_data['sesso'] = str(sesso).strip()
                                        
                                if 'telefono' in PAZIENTI_FIELDS:
                                    telefono = clean_dbf_value(record[PAZIENTI_FIELDS['telefono']])
                                    if telefono:
                                        paziente_data['telefono'] = str(telefono).strip()
                                        
                                if 'cellulare' in PAZIENTI_FIELDS:
                                    cellulare = clean_dbf_value(record[PAZIENTI_FIELDS['cellulare']])
                                    if cellulare:
                                        paziente_data['cellulare'] = str(cellulare).strip()
                                        
                                if 'email' in PAZIENTI_FIELDS:
                                    email = clean_dbf_value(record[PAZIENTI_FIELDS['email']])
                                    if email:
                                        paziente_data['email'] = str(email).strip()
                                        
                                if 'indirizzo' in PAZIENTI_FIELDS:
                                    indirizzo = clean_dbf_value(record[PAZIENTI_FIELDS['indirizzo']])
                                    if indirizzo:
                                        paziente_data['indirizzo'] = str(indirizzo).strip()
                                        
                                if 'citta' in PAZIENTI_FIELDS:
                                    citta = clean_dbf_value(record[PAZIENTI_FIELDS['citta']])
                                    if citta:
                                        paziente_data['citta'] = str(citta).strip()
                                        
                                if 'provincia' in PAZIENTI_FIELDS:
                                    provincia = clean_dbf_value(record[PAZIENTI_FIELDS['provincia']])
                                    if provincia:
                                        paziente_data['provincia'] = str(provincia).strip()
                                        
                                if 'cap' in PAZIENTI_FIELDS:
                                    cap = clean_dbf_value(record[PAZIENTI_FIELDS['cap']])
                                    if cap:
                                        paziente_data['cap'] = str(cap).strip()
                                        
                                if 'note' in PAZIENTI_FIELDS:
                                    note = clean_dbf_value(record[PAZIENTI_FIELDS['note']])
                                    if note:
                                        paziente_data['note'] = str(note).strip()
                                        
                                if 'ultima_visita' in PAZIENTI_FIELDS:
                                    ultima_visita = clean_dbf_value(record[PAZIENTI_FIELDS['ultima_visita']])
                                    if ultima_visita:
                                        paziente_data['ultima_visita'] = self._format_date_field(ultima_visita)
                                        
                                if 'mesi_richiamo' in PAZIENTI_FIELDS:
                                    mesi = clean_dbf_value(record[PAZIENTI_FIELDS['mesi_richiamo']])
                                    if mesi and str(mesi).isdigit():
                                        paziente_data['mesi_richiamo'] = int(mesi)
                                        
                                if 'tipo_richiamo' in PAZIENTI_FIELDS:
                                    tipo = clean_dbf_value(record[PAZIENTI_FIELDS['tipo_richiamo']])
                                    if tipo:
                                        paziente_data['tipo_richiamo'] = str(tipo).strip()
                                        
# … snippet truncated; inspect the source file for the remaining lines

--- C:\Users\NICOLA\Desktop\StudioDimaAI\server_v2\services\pazienti_service.py::get_pazienti_paginated (lines 47-196) ---
    def get_pazienti_paginated(self, page=1, per_page=50, filters=None) -> Dict[str, Any]:
        """Get paginated list of pazienti from DBF files."""
        try:
            # Get DBF file path using config manager
            pazienti_path = self._get_pazienti_dbf_path()
            
            if not os.path.exists(pazienti_path):
                self.logger.warning(f"Pazienti DBF file not found: {pazienti_path}")
                raise DatabaseError(f"Pazienti DBF file not found: {pazienti_path}")
            
            # Read DBF file directly using dbf library
            pazienti_raw = []
            with dbf.Table(pazienti_path, codepage='cp1252') as table:
                for record in table:
                    try:
                        # Extract fields using constants mapping - access directly by field name
                        paziente_id = clean_dbf_value(record[PAZIENTI_FIELDS['id']])
                        paziente_nome = clean_dbf_value(record[PAZIENTI_FIELDS['nome']])
                        
                        if paziente_id and paziente_nome:
                            # Extract all available fields
                            paziente_data = {
                                'id': str(paziente_id).strip(),
                                'nome': str(paziente_nome).strip()
                            }
                            
                            # Add optional fields if available
                            try:
                                if 'codice_fiscale' in PAZIENTI_FIELDS:
                                    cf = clean_dbf_value(record[PAZIENTI_FIELDS['codice_fiscale']])
                                    if cf:
                                        paziente_data['codice_fiscale'] = str(cf).strip()
                                        
                                if 'data_nascita' in PAZIENTI_FIELDS:
                                    data_nascita = clean_dbf_value(record[PAZIENTI_FIELDS['data_nascita']])
                                    if data_nascita:
                                        paziente_data['data_nascita'] = self._format_date_field(data_nascita)
                                        
                                if 'sesso' in PAZIENTI_FIELDS:
                                    sesso = clean_dbf_value(record[PAZIENTI_FIELDS['sesso']])
                                    if sesso:
                                        paziente_data['sesso'] = str(sesso).strip()
                                        
                                if 'telefono' in PAZIENTI_FIELDS:
                                    telefono = clean_dbf_value(record[PAZIENTI_FIELDS['telefono']])
                                    if telefono:
                                        paziente_data['telefono'] = str(telefono).strip()
                                        
                                if 'cellulare' in PAZIENTI_FIELDS:
                                    cellulare = clean_dbf_value(record[PAZIENTI_FIELDS['cellulare']])
                                    if cellulare:
                                        paziente_data['cellulare'] = str(cellulare).strip()
                                        
                                if 'email' in PAZIENTI_FIELDS:
                                    email = clean_dbf_value(record[PAZIENTI_FIELDS['email']])
                                    if email:
                                        paziente_data['email'] = str(email).strip()
                                        
                                if 'indirizzo' in PAZIENTI_FIELDS:
                                    indirizzo = clean_dbf_value(record[PAZIENTI_FIELDS['indirizzo']])
                                    if indirizzo:
                                        paziente_data['indirizzo'] = str(indirizzo).strip()
                                        
                                if 'citta' in PAZIENTI_FIELDS:
                                    citta = clean_dbf_value(record[PAZIENTI_FIELDS['citta']])
                                    if citta:
                                        paziente_data['citta'] = str(citta).strip()
                                        
                                if 'provincia' in PAZIENTI_FIELDS:
                                    provincia = clean_dbf_value(record[PAZIENTI_FIELDS['provincia']])
                                    if provincia:
                                        paziente_data['provincia'] = str(provincia).strip()
                                        
                                if 'cap' in PAZIENTI_FIELDS:
                                    cap = clean_dbf_value(record[PAZIENTI_FIELDS['cap']])
                                    if cap:
                                        paziente_data['cap'] = str(cap).strip()
                                        
                                if 'note' in PAZIENTI_FIELDS:
                                    note = clean_dbf_value(record[PAZIENTI_FIELDS['note']])
                                    if note:
                                        paziente_data['note'] = str(note).strip()
                                        
                                if 'ultima_visita' in PAZIENTI_FIELDS:
                                    ultima_visita = clean_dbf_value(record[PAZIENTI_FIELDS['ultima_visita']])
                                    if ultima_visita:
                                        paziente_data['ultima_visita'] = self._format_date_field(ultima_visita)
                                        
                                if 'mesi_richiamo' in PAZIENTI_FIELDS:
                                    mesi = clean_dbf_value(record[PAZIENTI_FIELDS['mesi_richiamo']])
                                    if mesi and str(mesi).isdigit():
                                        paziente_data['mesi_richiamo'] = int(mesi)
                                        
                                if 'tipo_richiamo' in PAZIENTI_FIELDS:
                                    tipo = clean_dbf_value(record[PAZIENTI_FIELDS['tipo_richiamo']])
                                    if tipo:
                                        paziente_data['tipo_richiamo'] = str(tipo).strip()
                                        
                                if 'da_richiamare' in PAZIENTI_FIELDS:
                                    da_richiamare = clean_dbf_value(record[PAZIENTI_FIELDS['da_richiamare']])
# … snippet truncated; inspect the source file for the remaining lines

--- C:\Users\NICOLA\Desktop\StudioDimaAI\server_v2\services\pazienti_service.py::search_pazienti (lines 226-269) ---
    def search_pazienti(self, query: str, limit: int = 20, telefono: str = '') -> Dict[str, Any]:
        """Search pazienti by name, codice fiscale or telefono."""
        try:
            pazienti_path = self._get_pazienti_dbf_path()

            if not os.path.exists(pazienti_path):
                raise DatabaseError(f"Pazienti DBF file not found: {pazienti_path}")

            results = []
            query_lower = query.lower().strip()
            telefono_clean = telefono.strip()

            with dbf.Table(pazienti_path, codepage='cp1252') as table:
                for record in table:
                    if len(results) >= limit:
                        break

                    if record[PAZIENTI_FIELDS['id']]:
                        match = False

                        if telefono_clean:
                            tel = str(clean_dbf_value(record[PAZIENTI_FIELDS['telefono']]))
                            cell = str(clean_dbf_value(record[PAZIENTI_FIELDS['cellulare']]))
                            match = telefono_clean in tel or telefono_clean in cell
                        elif query_lower:
                            nome = clean_dbf_value(record[PAZIENTI_FIELDS['nome']])
                            cf = clean_dbf_value(record[PAZIENTI_FIELDS['codice_fiscale']])
                            match = query_lower in nome.lower() or query_lower in cf.lower()

                        if match:
                            paziente = self._extract_paziente_data(record)
                            if paziente:
                                results.append(paziente)
            
            return {
                'success': True,
                'data': results,
                'count': len(results),
                'query': query
            }
            
        except Exception as e:
            self.logger.error(f"Error searching pazienti: {e}")
            raise DatabaseError(f"Failed to search pazienti: {str(e)}")

--- C:\Users\NICOLA\Desktop\StudioDimaAI\server_v2\api\v2_pazienti.py::get_pazienti (lines 79-135) ---
def get_pazienti():
    """
    Get list of patients with pagination and filtering.
    
    Query Parameters:
        page (int): Page number (default: 1)
        per_page (int): Items per page (default: 50, max: 100)
        search (str): Search term for name or codice fiscale
        da_richiamare (bool): Filter by patients to recall
        in_cura (bool): Filter by patients in care
        all (int): If 1, load all patients without pagination
    
    Returns:
        JSON response with paginated patients list
    """
    try:
        # Get query parameters
        page = request.args.get('page', 1, type=int)
        per_page = min(request.args.get('per_page', 50, type=int), 100)
        search = request.args.get('search', '').strip()
        da_richiamare = request.args.get('da_richiamare', type=bool)
        in_cura = request.args.get('in_cura', type=bool)
        load_all = request.args.get('all', 0, type=int)
        
        # Validate parameters
        validate_pagination_params(page, per_page)
        
        # If load_all is requested, set high per_page
        if load_all:
            per_page = 10000
            page = 1
        
        # Build filters
        filters = {}
        if search:
            filters['search'] = search
        if da_richiamare is not None:
            filters['da_richiamare'] = da_richiamare
        if in_cura is not None:
            filters['in_cura'] = in_cura
        
        # Get pazienti data
        service = PazientiService(g.database_manager)
        result = service.get_pazienti_paginated(
            page=page,
            per_page=per_page,
            filters=filters
        )
        
        # Return response
        if result['success']:
            return format_response(result['data'])
        else:
            return format_response(success=False, error=result.get('error', 'Errore caricamento pazienti'))
        
    except (ValidationError, DatabaseError, Exception) as e:
        return handle_error(e, "get_pazienti")

--- C:\Users\NICOLA\Desktop\StudioDimaAI\server_v2\api\v2_pazienti.py::search_pazienti (lines 168-203) ---
def search_pazienti():
    """
    Search patients by name, codice fiscale or telefono.

    Query Parameters:
        q (str): Search query (required)
        limit (int): Maximum results (default: 20, max: 100)
        telefono (str): Search by phone number (optional, alternative to q)

    Returns:
        JSON response with search results
    """
    try:
        query = request.args.get('q', '').strip()
        telefono = request.args.get('telefono', '').strip()
        limit = min(request.args.get('limit', 20, type=int), 100)

        if not query and not telefono:
            raise ValidationError("Search query is required")
        if query and len(query) < 2:
            raise ValidationError("Search query must be at least 2 characters")

        service = PazientiService(g.database_manager)
        result = service.search_pazienti(query, limit, telefono=telefono)
        
        if result['success']:
            return format_response({
                'pazienti': result['data'],
                'count': result['count'],
                'query': result['query']
            })
        else:
            return format_response(success=False, error=result.get('error', 'Errore ricerca pazienti'))
        
    except (ValidationError, DatabaseError, Exception) as e:
        return handle_error(e, "search_pazienti")

--- C:\Users\NICOLA\Desktop\StudioDimaAI\server_v2\api\v2_pazienti.py::get_pazienti_stats (lines 208-244) ---
def get_pazienti_stats():
    """
    Get patients statistics.
    
    Returns:
        JSON response with statistics
    """
    try:
        service = PazientiService(g.database_manager)
        
        # Get all patients for stats (using high per_page)
        result = service.get_pazienti_paginated(page=1, per_page=10000)
        
        if not result['success']:
            return format_response(success=False, error=result.get('error', 'Errore caricamento pazienti')), 500
        
        pazienti = result['data']['pazienti']
        
        # Calculate statistics
        stats = {
            'totale_pazienti': len(pazienti),
            'da_richiamare': len([p for p in pazienti if p.get('da_richiamare') == 'S']),
            'in_cura': len([p for p in pazienti if not p.get('non_in_cura', False)]),
            'non_in_cura': len([p for p in pazienti if p.get('non_in_cura', False)]),
            'con_email': len([p for p in pazienti if p.get('email')]),
            'con_cellulare': len([p for p in pazienti if p.get('cellulare')])
        }
        
        return format_response(stats)
        
    except Exception as e:
        logger.error(f"Error in get_pazienti_stats: {e}")
        return format_response({
            'success': False,
            'error': 'INTERNAL_ERROR',
            'message': 'Internal server error'
        }, 500)

--- C:\Users\NICOLA\Desktop\StudioDimaAI\server_v2\scripts\seed_patient_education.py::seed_patient_education (lines 16-128) ---
def seed_patient_education():
    try:
        db_manager = get_database_manager()
        repo = SocialMediaRepository(db_manager)

        # 1. Trova ID categoria 'Educazione Pazienti'
        cat = repo.execute_custom_query(
            "SELECT id FROM content_categories WHERE name = ?",
            ('Educazione Pazienti',),
            fetch_one=True
        )
        
        if not cat:
            logger.error("Categoria 'Educazione Pazienti' non trovata. Esegui prima seed_social_media.py")
            return

        cat_id = cat['id']
        logger.info(f"Trovata categoria 'Educazione Pazienti' con ID: {cat_id}")

        # 2. Crea Template
        templates = [
            {
                'name': 'Consigli Igiene Orale',
                'description': 'Template per consigli settimanali su igiene orale',
                'category_id': cat_id,
                'template_type': 'social_post',
                'subject': 'Consiglio del giorno',
                'body': "🦷 **Consiglio del Dottore** 🦷\n\nSapevi che {argomento}?\n\nÈ importante perché {motivo}.\n\nPrenota una visita di controllo per saperne di più! 📞 0123-456789\n\n#Dentista #SaluteOrale #Prevenzione #{argomento_hashtag}",
                'variables': json.dumps(['argomento', 'motivo', 'argomento_hashtag']),
                'is_active': 1
            },
            {
                'name': 'Promemoria Controllo',
                'description': 'Post per ricordare l\'importanza dei controlli periodici',
                'category_id': cat_id,
                'template_type': 'social_post',
                'subject': 'È ora del controllo?',
                'body': "📅 **Hai fatto il controllo semestrale?**\n\nLa prevenzione è la cura migliore! Non aspettare di avere dolore.\n\nContattaci oggi stesso per fissare il tuo appuntamento.\n\n📍 Studio Dima\n📞 0123-456789\n🌐 www.studiodima.it",
                'variables': '[]',
                'is_active': 1
            }
        ]

        logger.info("--- Creazione Template ---")
        for tpl in templates:
            # Check if exists
            exists = repo.execute_custom_query(
                "SELECT id FROM content_templates WHERE name = ?", 
                (tpl['name'],), 
                fetch_one=True
            )
            if not exists:
                columns = ', '.join(tpl.keys())
                placeholders = ', '.join(['?'] * len(tpl))
                sql = f"INSERT INTO content_templates ({columns}) VALUES ({placeholders})"
                with db_manager.transaction() as conn:
                    conn.execute(sql, tuple(tpl.values()))
                logger.info(f"Creato template: {tpl['name']}")
            else:
                logger.info(f"Template esistente: {tpl['name']}")

        # 3. Crea Post (Bozze e Programmati)
        posts = [
            {
                'title': 'L\'importanza del filo interdentale',
                'content': "Non basta spazzolare! 🪥\n\nIl filo interdentale raggiunge dove lo spazzolino non arriva, rimuovendo la placca tra i denti e sotto il bordo gengivale.\n\nUsalo almeno una volta al giorno, preferibilmente la sera prima di dormire.\n\n#FiloInterdentale #IgieneOrale #StudioDima",
                'category_id': cat_id,
                'content_type': 'post',
                'status': 'draft',
                'platforms': ['instagram', 'facebook'],
                'hashtags': ['#FiloInterdentale', '#IgieneOrale', '#Prevenzione']
            },
            {
                'title': 'Come prevenire la carie nei bambini',
                'content': "👶 **Sorrisi sani fin da piccoli**\n\n1. Inizia a pulire le gengive anche prima che spuntino i denti.\n2. Evita bevande zuccherate nel biberon.\n3. Prima visita dal dentista entro il 1° anno.\n\nLa salute inizia da piccoli! 🦷\n\n#Bambini #DentiDaLatte #MammeItaliane",
                'category_id': cat_id,
                'content_type': 'post',
                'status': 'scheduled',
                'scheduled_at': (datetime.utcnow() + timedelta(days=2)).isoformat(),
                'platforms': ['facebook'],
                'hashtags': ['#Pedodonzia', '#SaluteBambini']
            },
             {
                'title': 'Sintomi della Gengivite',
                'content': "🚨 **Gengive che sanguinano?**\n\nPotrebbe essere un primo segnale di gengivite. Se le tue gengive sono rosse, gonfie o sanguinano quando spazzoli, non ignorarlo!\n\nLa gengivite è reversibile se trattata in tempo. Prenota un'igiene professionale.\n\n#Gengivite #SaluteGengivale #IgienistaMentale",
                'category_id': cat_id,
                'content_type': 'post',
                'status': 'draft',
                'platforms': ['instagram', 'linkedin'],
                'hashtags': ['#GengiveSane', '#NoSangue']
            }
        ]

        logger.info("\n--- Creazione Post ---")
        for post_data in posts:
            # Check duplicate by title
            exists = repo.execute_custom_query(
                "SELECT id FROM posts WHERE title = ?",
                (post_data['title'],),
                fetch_one=True
# … snippet truncated; inspect the source file for the remaining lines

--- C:\Users\NICOLA\Desktop\StudioDimaAI\client_v2\src\features\pazienti\pages\PazientiPage.tsx::getCampiAnagrafica (lines 53-67) ---
  const getCampiAnagrafica = (paziente: Paziente) => [
    { label: 'Nome', value: paziente.nome || '-' },
    { label: 'Codice Fiscale', value: paziente.codice_fiscale || '-' },
    { label: 'Data di Nascita', value: paziente.data_nascita || '-' },
    { label: 'Luogo di Nascita', value: paziente.luogo_nascita || '-' },
    { label: 'Sesso', value: paziente.sesso || '-' },
    { label: 'Indirizzo', value: paziente.indirizzo || '-' },
    { label: 'Citta', value: paziente.citta || '-' },
    { label: 'CAP', value: paziente.cap || '-' },
    { label: 'Provincia', value: paziente.provincia || '-' },
    { label: 'Telefono', value: paziente.telefono || '-' },
    { label: 'Email', value: paziente.email || '-' },
    { label: 'Tessera Sanitaria', value: paziente.tessera_sanitaria || '-' },
    { label: 'Note', value: paziente.note || '-' },
  ];

--- C:\Users\NICOLA\Desktop\StudioDimaAI\client_v2\src\features\pazienti\pages\RichiamiPage.tsx::sortIcon (lines 179-180) ---
  const sortIcon = (col: typeof sortCol) =>
    sortCol === col ? (sortAsc ? ' ▲' : ' ▼') : '';

=== CONNECTIONS ===
- client_v2/src/features/pazienti/components/BroadcastModal.tsx
  internal:
    - client_v2/src/store/pazienti.store.tsx
- client_v2/src/features/pazienti/components/FiltriPazienti.tsx
  internal:
    - client_v2/src/components/selects/SlimSelect.tsx
    - client_v2/src/store/pazienti.store.tsx
- client_v2/src/features/pazienti/components/ModalRichiamoGestione.tsx
  internal:
    - client_v2/src/store/pazienti.store.tsx
- client_v2/src/features/pazienti/components/PazientiTable.tsx
  internal:
    - client_v2/src/components/tables/DataTable.tsx
    - client_v2/src/features/pazienti/components/RichiamoStatus.tsx
    - client_v2/src/store/pazienti.store.tsx
- client_v2/src/features/pazienti/components/RichiamoGestione.tsx
  internal:
    - client_v2/src/store/pazienti.store.tsx
- client_v2/src/features/pazienti/components/RichiamoStatus.tsx
  internal:
    - client_v2/src/store/pazienti.store.tsx
- client_v2/src/features/pazienti/pages/PazientiPage.tsx
  internal:
    - client_v2/src/components/layout/PageLayout.tsx
    - client_v2/src/components/modals/index.ts
    - client_v2/src/features/pazienti/components/PazientiTable.tsx
    - client_v2/src/store/pazienti.store.tsx
- client_v2/src/features/pazienti/pages/RichiamiPage.tsx
  internal:
    - client_v2/src/components/layout/PageLayout.tsx
    - client_v2/src/features/pazienti/services/richiami.service.ts
    - client_v2/src/services/api/client.ts
- client_v2/src/features/pazienti/pages/SlotLiberiPage.tsx
  internal:
    - client_v2/src/components/layout/PageLayout.tsx
- client_v2/src/features/pazienti/services/marketing.service.ts
  internal:
    - client_v2/src/services/api/client.ts
- client_v2/src/features/pazienti/services/richiami.service.ts
  internal:
    - client_v2/src/services/api/client.ts
- client_v2/src/store/pazienti.store.tsx
  internal:
    - client_v2/src/services/api/client.ts